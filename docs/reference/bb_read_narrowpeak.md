# A Function to Read In Narrow Peaks Files

This function reads the narrow peaks file (BED6 + 4 format) and turns it
into a GRanges object.

## Usage

``` r
bb_read_narrowpeak(file)
```

## Arguments

- file:

  The file path to the narrow peaks file.

## Value

A GRanges object
