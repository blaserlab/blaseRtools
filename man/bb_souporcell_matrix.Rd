% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bb_souporcell_matrix.R
\name{bb_souporcell_matrix}
\alias{bb_souporcell_matrix}
\title{Build a Souporcell genotype-by-SNP allele-fraction matrix}
\usage{
bb_souporcell_matrix(
  souporcell_dir,
  return = c("continuous", "discrete"),
  orientation = c("snps_by_genotypes", "genotypes_by_snps"),
  variant_file = NULL,
  use_variant_names = TRUE,
  variant_name_style = c("chr_pos_ref_alt", "chr_pos", "id"),
  status_keep = "singlet",
  assignment_col = "assignment",
  status_col = "status",
  min_total_reads = 10,
  min_genotype_coverage = 0.7,
  require_finite = TRUE,
  min_range = 0.35,
  min_sd = 0.12,
  ref_threshold = 0.2,
  alt_threshold = 0.8,
  require_ref_like = 1L,
  require_alt_like = 1L,
  top_n = 75L,
  discrete_values = c(ref = 0, het = 0.5, alt = 1),
  genotype_prefix = "ID_",
  snp_prefix = "SNP_",
  sort_genotypes = TRUE,
  verbose = TRUE,
  return_metadata = FALSE
)
}
\arguments{
\item{souporcell_dir}{Character scalar. Path to a Souporcell output directory,
for example \code{"results/g_pos_1/souporcell/K15"}.}

\item{return}{Character scalar. One of \code{"continuous"} or \code{"discrete"}.
\code{"continuous"} returns aggregate alternate allele fractions. \code{"discrete"}
returns genotype-like binned values.}

\item{orientation}{Character scalar. One of \code{"snps_by_genotypes"} or
\code{"genotypes_by_snps"}. The default returns SNPs as rows and Souporcell
genotype IDs as columns.}

\item{variant_file}{Character scalar or \code{NULL}. Path to the variant file used
for \code{alt.mtx} / \code{ref.mtx} column order. If \code{NULL}, the function looks for
\code{common_variants_covered_tmp.vcf} in \code{souporcell_dir}.}

\item{use_variant_names}{Logical scalar. If \code{TRUE}, use variant coordinates
from \code{variant_file} as SNP names.}

\item{variant_name_style}{Character scalar. One of \code{"chr_pos_ref_alt"},
\code{"chr_pos"}, or \code{"id"}.}

\item{status_keep}{Character vector. Cell statuses from \code{clusters.tsv} to
include. Default is \code{"singlet"}.}

\item{assignment_col}{Character scalar. Column in \code{clusters.tsv} containing
Souporcell genotype assignments. Default is \code{"assignment"}.}

\item{status_col}{Character scalar. Column in \code{clusters.tsv} containing cell
status. Default is \code{"status"}.}

\item{min_total_reads}{Numeric scalar. Minimum aggregate read depth required
for a genotype/SNP pair. Values below this are set to \code{NA} before SNP
filtering.}

\item{min_genotype_coverage}{Numeric scalar between 0 and 1. Fraction of
genotype IDs that must have adequate coverage for a SNP to be retained.}

\item{require_finite}{Logical scalar. If \code{TRUE}, remove SNPs with any
remaining non-finite values after coverage filtering. Recommended for
heatmap clustering.}

\item{min_range}{Numeric scalar. Minimum ALT allele-fraction range across
genotype IDs required for a SNP to be retained.}

\item{min_sd}{Numeric scalar. Minimum ALT allele-fraction standard deviation
across genotype IDs required for a SNP to be retained.}

\item{ref_threshold}{Numeric scalar. ALT allele fractions less than or equal
to this value are considered reference-like.}

\item{alt_threshold}{Numeric scalar. ALT allele fractions greater than or
equal to this value are considered alternate-like.}

\item{require_ref_like}{Integer scalar. Minimum number of genotype IDs that
must be reference-like for a SNP to be retained.}

\item{require_alt_like}{Integer scalar. Minimum number of genotype IDs that
must be alternate-like for a SNP to be retained.}

\item{top_n}{Integer scalar or \code{NULL}. If non-\code{NULL}, keep only the top
\code{top_n} SNPs ranked by allele-fraction range and then standard deviation.}

\item{discrete_values}{Numeric vector of length 3. Values used for
reference-like, intermediate/heterozygous-like, and alternate-like states
when \code{return = "discrete"}.}

\item{genotype_prefix}{Character scalar. Prefix for genotype names.}

\item{snp_prefix}{Character scalar. Prefix for fallback SNP names when no
usable variant label is available.}

\item{sort_genotypes}{Logical scalar. If \code{TRUE}, sort genotype IDs
numerically when possible.}

\item{verbose}{Logical scalar. If \code{TRUE}, print filtering summaries.}

\item{return_metadata}{Logical scalar. If \code{TRUE}, return a list containing the
matrix and intermediate summary tables. If \code{FALSE}, return only the matrix.}
}
\value{
If \code{return_metadata = FALSE}, a numeric matrix. By default, rows are
SNPs and columns are Souporcell genotype IDs. If \code{return_metadata = TRUE},
a list with elements \code{matrix}, \code{snp_summary}, \code{variant_table},
\code{alt_by_genotype}, \code{ref_by_genotype}, \code{depth_by_genotype},
\code{allele_fraction_by_genotype}, and \code{clusters}.
}
\description{
Reads standard Souporcell output files and aggregates reference/alternate
allele counts across cells assigned to each Souporcell genotype. The returned
matrix is designed for genotype-demultiplexing QC plots, especially heatmaps
showing whether inferred genetic IDs have distinct SNP allele profiles.
}
\details{
The function expects a Souporcell output directory containing \code{alt.mtx},
\code{ref.mtx}, and \code{clusters.tsv}. If present, \code{common_variants_covered_tmp.vcf}
is used to label SNPs with genomic coordinates and alleles.
}
\examples{
mat <- build_souporcell_genotype_matrix(
  souporcell_dir = "results/g_pos_1/souporcell/K15",
  return = "discrete",
  orientation = "snps_by_genotypes",
  top_n = 75
)

pheatmap::pheatmap(
  mat,
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  show_rownames = FALSE
)

}
