# Plot The X Axis From A Trace Object

Generates the X axis for stacking other track plots on top of.

## Usage

``` r
bb_plot_trace_axis(trace, xtitle = NULL)
```

## Arguments

- trace:

  A Trace object.

- xtitle:

  An optional title for the X axis. Defaults to the genome and
  chromosome.
